<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Wszystkie złożone zamówienia</title>
</head>
<body>
  <h1>Wszystkie złożone zamówienia</h1>
  <% if (orders.length > 0) { %>
    <ul>
      <% let currentOrderId = null; %>
      <% orders.forEach(order => { %>
        <% if (currentOrderId !== order.order_id) { %>
          <% currentOrderId = order.order_id; %>
          <h2>Zamówienie ID: <%= order.order_id %> (Użytkownik: <%= order.user_name %>)</h2>
          <p>Data: <%= new Date(order.created_at).toLocaleString() %></p>
        <% } %>
        <li>
          Produkt: <%= order.product_name %><br>
          Opis: <%= order.product_description %><br>
          Cena: <%= order.product_price.toFixed(2) %> zł<br>
          Ilość: <%= order.quantity %>
        </li>
      <% }) %>
    </ul>
  <% } else { %>
    <p>Brak złożonych zamówień.</p>
  <% } %>
</body>
</html>
