<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Open orders</title>
</head>
<body>
  <h1>Open orders, still in the user's carts</h1>

  <% if (orders.length === 0) { %>
    <p>There are no open orders now.</p>
  <% } else { %>
    <table border="1">
      <thead>
        <tr>
          <th>Username</th>
          <th>Total Price</th>
          <th>Total Quantity</th>
          <th>Product IDs</th>
        </tr>
      </thead>
      <tbody>
        <% orders.forEach(order => { %>
          <tr>
            <td><%= order.user_name %></td>
            <td><%= order.total_price %> zł</td> 
            <td><%= order.total_quantity %></td> 
            <td><%= order.product_ids.join(', ') %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  <% } %>
  <br>
  <br /><br />
  <a href="/">Back to the index page</a>
  <br /><br />
  <a href="/admin/users">See users list</a>
  <br /><br />
  <a href="/admin/placed">Check placed orders</a>
</body>
</html>